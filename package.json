{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n gateway,frontend \"npm run dev --workspace gateway\" \"npm run dev --workspace frontend\""
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "concurrently": "^8.2.2"
  }
}
