{
  "name": "explorer-slim",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^8.0.0",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "prepare": "husky install",
    "backend": "pnpm --filter backend dev",
    "frontend": "pnpm --filter frontend dev"
  },
  "dependencies": {

  }
}
