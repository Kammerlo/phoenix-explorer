# =============================================================================
# Phoenix Explorer — environment configuration
# Copy this file to .env at the repo root:
#   cp .env.example .env
# Then fill in the required values below.
# =============================================================================

# ---------------------------------------------------------------------------
# Gateway
# ---------------------------------------------------------------------------

# Blockfrost project API key.
# Obtain a key at https://blockfrost.io
# The key prefix must match NETWORK (e.g. "mainnet..." for mainnet).
# Optional only when DEMETER_URL + DEMETER_API_KEY are set, but required for
# /api/pools/* — demeter.run does not implement the pool endpoints, so when
# only demeter is configured the pool routes return a 501 "not supported".
API_KEY=

# demeter.run hosted Blockfrost extension (https://demeter.run).
# When both DEMETER_URL and DEMETER_API_KEY are set, the gateway prefers
# demeter for every endpoint except /api/pools/* (which always goes to
# blockfrost.io if API_KEY is present).
# DEMETER_URL example: https://cardano-mainnet.blockfrost.m1.demeter.run/api/v0
DEMETER_URL=
DEMETER_API_KEY=

# Port the Express gateway listens on (default: 3000)
PORT=3000

# Interface the gateway binds to (default: 0.0.0.0)
HOST=0.0.0.0

# Cardano network for Blockfrost: mainnet | preprod | preview
NETWORK=mainnet

# ---------------------------------------------------------------------------
# Frontend
# ---------------------------------------------------------------------------

# Active data provider: GATEWAY | YACI | BLOCKFROST
# GATEWAY    — proxy requests through the local Express gateway (recommended)
# YACI       — connect directly to a Yaci Store REST API
# BLOCKFROST — connect directly to Blockfrost from the browser (exposes API key!)
REACT_APP_API_TYPE=GATEWAY

# Base URL for the selected provider
# GATEWAY example:    http://localhost:3000/api
# YACI example:       http://localhost:8080/api/v1
# BLOCKFROST example: https://cardano-mainnet.blockfrost.io/api/v0
REACT_APP_API_URL=http://localhost:3000/api

# Network label shown in the UI: mainnet | preprod | preview
REACT_APP_NETWORK=mainnet

# Blockfrost API key for direct browser calls (only used when REACT_APP_API_TYPE=BLOCKFROST)
# Warning: this value will be visible in the compiled JavaScript bundle
REACT_APP_BLOCKFROST_API_KEY=

# CoinGecko endpoint for ADA price data (leave as-is unless self-hosting)
REACT_APP_API_URL_COIN_GECKO=https://api.coingecko.com/api/v3/coins/markets
